<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kpz_chess_2" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kpzchess2" >
        <activity android:name=".activities.navigationSettings.navigationSettingsActivity" >
        </activity>
        <activity
            android:name=".activities.navigationSettings.NavigationSettingsActivity"
            android:label="@string/title_activity_navigation_settings"
            android:theme="@style/Theme.Kpzchess2.NoActionBar" />
        <activity android:name=".activities.userSettings.UserSettingsActivity" />
        <activity android:name=".activities.menu.MenuActivity" />
        <activity android:name=".activities.game.GameActivity" />
        <activity
            android:name=".activities.settings.SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>